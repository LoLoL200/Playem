import 'package:flutter/material.dart';

final homeAppBarlight = TextStyle(
  fontSize: 32, 
  //decorationColor: Colors.black54,
  shadows: <Shadow>[
    Shadow(
      offset: Offset(2, 2),
      blurRadius: 2,
      color: Colors.white
    )
  ]
  );
final homeAppBardark = TextStyle(
  fontSize: 32, 
  //decorationColor: Colors.black54,
  shadows: <Shadow>[
    Shadow(
      offset: Offset(2, 2),
      blurRadius: 2,
      color:  Colors.black
    )
  ]
  );

  final homelight = TextStyle(
  fontSize: 16, 
  //decorationColor: Colors.black54,
  shadows: <Shadow>[
    Shadow(
      offset: Offset(1, 1),
      blurRadius: 2,
      color:  Colors.white
    )
  ]
  );
final homedark = TextStyle(
  fontSize: 16, 
  //decorationColor: Colors.black54,
  shadows: <Shadow>[
    Shadow(
      offset: Offset(2, 2),
      blurRadius: 2,
      color:  Colors.black
    )
  ]
  );