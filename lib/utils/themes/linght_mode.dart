import 'package:flutter/material.dart';

ThemeData linghtMode = ThemeData(
  colorScheme: ColorScheme.light(
    background:  Colors.deepOrange,
    primary: Colors.black,
    secondary: Colors.deepOrange,
    inversePrimary: Colors.black,
  )
);